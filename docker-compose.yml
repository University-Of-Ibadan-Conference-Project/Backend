version: '3.9'

services:
  backend:
    build:
      context: ./backend/
    restart: on-failure
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/backend
    command: >
      sh -c "
        python manage.py migrate --noinput &&
        python manage.py collectstatic --noinput &&
        python manage.py runserver 0.0.0.0:8000"
    env_file:
      - ./.env

  # IGNORED FOR NOW
  # frontend:
  #   build:
  #     context: ./frontend/
  #   restart: on-failure
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./frontend:/frontend
  #     - /frontend/node_modules
  #   command: >
  #     sh -c "npm start"
  #   env_file:
  #     - ./.env
  #   depends_on:
  #     - backend

  # Admin dashboard (Vite dev server). Uncomment to run it via compose; for
  # day-to-day work `cd admin-manager && npm run dev` is faster. The anonymous
  # node_modules volume keeps the Linux build separate from your host's.
  # admin-manager:
  #   image: node:22-alpine
  #   working_dir: /app
  #   restart: on-failure
  #   ports:
  #     - "5173:5173"
  #   volumes:
  #     - ./admin-manager:/app
  #     - /app/node_modules
  #   command: >
  #     sh -c "npm install && npm run dev -- --host 0.0.0.0"
  #   environment:
  #     - VITE_ADMIN_PASSCODE=${VITE_ADMIN_PASSCODE:-change-me}

volumes:
  pgdata: {}