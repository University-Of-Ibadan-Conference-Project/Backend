# Passcode that unlocks the admin dashboard.
# Copy this file to `.env.local` (gitignored) and set a real value, then
# restart the dev server. This is a lightweight gate, not real security —
# the value ships in the client bundle.
VITE_ADMIN_PASSCODE=change-me
